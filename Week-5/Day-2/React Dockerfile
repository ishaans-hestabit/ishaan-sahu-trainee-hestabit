FROM node:alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

# CMD ["npm", "run", "dev"]

CMD ["npm", "run", "dev", "--", "--host"]

# docker build -t react-client -f "React Dockerfile" client (this means everything inside this client folder)
# docker run -p 5173:5173 react-client
